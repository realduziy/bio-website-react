services:
  website-test:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: website_test
    ports:
      - "127.0.0.1:8000:80"
    restart: always
    volumes:
      - ./assets:/usr/share/nginx/html/assets
